# Modal Component

- Template tabanlı modal aç/kapa; `.c-modal__backdrop` ve `.c-modal` BEM blokları ile tanımlandı.
- `modal.js` veri attribute ( `data-modal-trigger` ) üzerinden event delegation, focus trap ve Escape kapanışı sağlar.
