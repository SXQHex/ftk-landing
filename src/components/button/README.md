# Button Component

- `.c-button` soyut CTA yapısı; `--primary` ve `--ghost` modifier'ları gradient/outline durumlarını yönetir.
- JS tarafında etkileşim bulunmadığından `initButtons` yalnızca mimari tutarlılık için expose edilir.
