# Hero Component

- İki kolonlu grid düzeni `.c-hero__grid` ile responsive çalışır, tek breakpoint mixin’i kullanır.
- Görsel `picture` + `srcset`/`sizes` üzerinden AVIF/WEBP/JPEG varyantlarını kullanır; `hero.js` şu an sadece mimari bütünlük için boş init sunar.
